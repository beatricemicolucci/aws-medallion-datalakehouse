{
  "name": "gold-loader",
  "version": "1.0.0",
  "description": "Incremental, config-driven loader from Athena (Silver) to PostgreSQL (Gold)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-athena": "^3.600.0",
    "dotenv": "^16.4.5",
    "pg": "^8.11.5",
    "pg-copy-streams": "^7.0.0"
  }
}
