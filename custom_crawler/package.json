{
  "name": "aws-glue-custom-parquet-crawler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Discovers Parquet datasets in S3 and synchronizes them to the AWS Glue Data Catalog.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-glue": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "duckdb": "^1.4.4",
    "hyparquet": "^1.30.0"
  }
}
